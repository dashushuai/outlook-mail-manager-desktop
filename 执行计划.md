# Outlook Mail Manager — Windows 桌面版 执行计划

> 基于 `重构方案-Windows桌面版.md`，采用 **Electron 渐进式重构**路线
> 代码复用率：前端 ~95%、后端 ~90%
> 总工时：140-220 小时，分三个阶段执行

## 当前进度（2026-05-12）

- SQLite 迁移前置工作已完成，当前开发工作区为 `.worktrees/sqlite3-migration`。
- 第一阶段“最小可用原型”已完成：Electron 壳层、预加载桥、嵌入式 Koa 服务、桌面端配置/鉴权适配、动态 API base、Windows NSIS 打包均已落地并通过阶段性验证。
- 第二阶段“系统托盘”已完成：关闭到托盘、右键菜单、双击显示主窗口、托盘图标 fallback、打包启动回归均已处理。
- 第二阶段“原生通知”已完成：`desktop:notify` IPC、preload `desktopShell.notify`、通知点击聚焦主窗口、IPC payload 边界校验、完整 Electron 测试、构建、打包、packaged smoke 与复审均已通过。
- 第二阶段“配置持久化 / 窗口状态记忆”已完成：已接入 `electron-store`，持久化窗口 bounds、主题偏好、开机自启、最小化到托盘、自动检查间隔；启动恢复窗口位置/大小，关闭前保存 bounds，并处理显示器布局变化导致的离屏窗口恢复问题。
- 第二阶段“开机自启动 / 邮件后台自动拉取”已完成：桌面设置入口、开机自启动配置、后台定时拉取、新邮件通知均已落地并通过完整验证。
- 第三阶段“自动更新”已完成：已集成 `electron-updater`，配置 GitHub Releases 发布源，提供启动检查、下载进度、安装确认和桌面设置入口。
- 第三阶段“文件对话框集成”已完成：账户导入使用原生打开对话框，账户导出使用原生保存对话框；preload 已暴露文件对话框 API，IPC payload / 返回值边界校验与 Web fallback 均已落地；备份恢复支持原生选择 `.db` 文件。
- 第三阶段“全局快捷键”已完成：`Ctrl+Shift+O` 切换主窗口显示/隐藏，`Ctrl+Shift+M` 手动拉取邮件；注册失败日志、退出注销、Electron 测试、构建、打包和 packaged smoke 均已通过。
- 第三阶段“应用图标”已完成：已生成包含 16/32/48/256 尺寸的 `build/icon.ico`，并通过 `electron-builder.yml` 的 `win.icon` 配置到 Windows 应用和安装包；图标测试、Electron 测试、构建、打包和 packaged smoke 均已通过。
- 第三阶段“安装包配置”已完成：Electron Builder 同时生成 NSIS 安装器和便携版 zip，已配置 `.json` 账户导入文件关联；账户页支持拖拽 `.json` 文件并复用现有导入预览/确认流程。
- 第三阶段“跨版本兼容性测试”已完成当前范围验证：Windows 11 Pro 64 位（10.0.26200）下 Electron 回归测试、JSON 导入测试、安装包产物检查和 packaged smoke 均已通过；Windows 10 兼容性验证经确认不再执行。
- 安装/卸载流程手动验收经确认跳过，不在当前机器运行安装器或卸载器。
- 自动更新端到端验证已完成发布前准备检查：本地 `latest.yml`、安装器、blockmap 与便携 zip 已生成且文件名一致；GitHub Releases 当前为空，用户确认先不发布，因此真实端到端更新验证延后到发布时执行。
- 发布前最终回归检查已完成：Electron 测试 65/65、server 测试 20/20、focused web 测试 16/16 均通过；Web 生产构建、Electron release 打包、release 产物/更新元数据检查和 packaged smoke 均通过。
- 下次续点：创建 GitHub Release 并补跑自动更新端到端验证，或整理提交/发布说明。

### 下一步优先待办

1. 创建 GitHub Release 并补跑自动更新端到端验证。
2. 整理提交/发布说明。

---

## 前置准备

### 环境搭建
- [ ] 克隆仓库到工作目录
- [ ] `npm run install:all` 安装所有依赖
- [ ] 安装 Electron 相关依赖：`electron`, `electron-builder`, `electron-store`, `electron-updater`, `concurrently`, `wait-on`
- [ ] 安装 Visual Studio Build Tools（编译 `better-sqlite3` 原生模块）
- [ ] 验证 Web 版可正常启动（`npm start`）
- [ ] Azure AD 应用注册中新增重定向 URI：`http://localhost:18000/api/auth/callback`（及 18001-18005 备选端口）

---

## 第一阶段：最小可用原型（第 1-2 周）

### 目标
双击 `.exe` 启动，所有现有功能正常工作。

### 1.1 创建 Electron 壳层目录结构
- [x] 创建 `electron/` 目录及以下文件：
  - `electron/main.ts` — 主进程入口
  - `electron/preload.ts` — 预加载脚本
  - `electron/server.ts` — Koa 服务启动包装
  - `electron/ipc/index.ts` — IPC 处理器注册

### 1.2 实现主进程 (`electron/main.ts`)
- [x] 在 `app.on('ready')` 中启动 Koa 服务
- [x] Koa 服务监听 `127.0.0.1`，使用 `server.listen(0)` 随机分配端口
- [x] 创建 `BrowserWindow`，加载动态 renderer 目标
- [x] 窗口就绪后再显示，避免白屏
- [x] 在应用退出流程中关闭服务
- [x] 安全配置：`contextIsolation: true`, `nodeIntegration: false`

### 1.3 实现预加载脚本 (`electron/preload.ts`)
- [x] 通过 `contextBridge` 暴露 API：
  - `desktopShell.ping()` — IPC 连通性检查
  - `desktopShell.getServerUrl()` — 获取嵌入式服务地址
  - `desktopShell.notify()` — 触发原生通知

### 1.4 实现 Koa 服务包装 (`electron/server.ts`)
- [x] 导入现有 `server/dist/` 中的 Koa app
- [x] 注入桌面运行环境变量
- [x] 注入 `ELECTRON_USER_DATA_PATH`，桌面数据库固定到 `app.getPath('userData')/outlook.db`

### 1.5 适配后端代码
- [x] `server/src/config/index.ts` — 桌面数据库路径支持 Electron userData
- [x] `server/src/middlewares/auth.ts` — 跳过访问密码鉴权（桌面版不需要）
- [x] `web/src/lib/api.ts` — API base URL 支持 file renderer 下的嵌入式服务地址

### 1.6 配置打包
- [x] 创建 `electron-builder.yml`
  - appId: `com.outlook-mail-manager.app`
  - Windows 目标: NSIS (x64)
  - 允许自定义安装路径

### 1.7 更新 `package.json`
- [x] 新增脚本：`electron:dev`, `electron:build`, `electron:start`
- [x] 新增 `main` 字段指向 `dist-electron/main.js`

### 第一阶段验证标准
- [x] `npm run electron:dev` 启动后窗口正常显示
- [x] 仪表盘、账户 CRUD、邮件拉取链路完成阶段性验证
- [x] 代理管理、OAuth2 认证流程沿用 Web 版链路
- [x] `npm run electron:build` 生成 Windows NSIS 安装包

---

## 第二阶段：桌面体验增强（第 3-4 周）

### 目标
从"能跑"到"好用"，增加桌面应用应有的体验。

### 2.1 系统托盘 (`electron/tray.ts`)
- [x] 关闭窗口时最小化到托盘，不退出应用
- [x] 托盘图标右键菜单：显示主窗口 / 检查邮件 / 退出
- [x] 托盘图标双击显示主窗口
- [x] 托盘图标加载失败时 fallback 到空图标，避免 packaged app 启动失败

### 2.2 原生通知 (`electron/notification.ts` + `electron/ipc/index.ts`)
- [x] 新邮件到达时通过 IPC 触发 Windows 原生通知
- [x] 点击通知聚焦主窗口
- [x] IPC payload 边界校验：拒绝空标题、非字符串标题、过长标题/正文
- [x] 完整验证：`npm run test:electron`、构建、打包、packaged smoke、复审

### 2.3 配置持久化
- [x] 用 `electron-store` 替代 `.env` 文件
- [x] 持久化项：窗口位置/大小、主题偏好、开机自启、最小化到托盘、自动检查邮件间隔

### 2.4 窗口状态记忆
- [x] 关闭前保存窗口 bounds 到 `electron-store`
- [x] 启动时恢复上次位置和大小

### 2.5 开机自启动
- [x] `app.setLoginItemSettings({ openAtLogin: true })` 可配置

### 2.6 邮件后台自动拉取
- [x] 主进程定时器定期调用邮件拉取 API
- [x] 有新邮件时发送通知

### 第二阶段验证标准
- [x] 关闭窗口后应用出现在系统托盘
- [x] 托盘右键菜单正常
- [x] 原生通知完整回归与 packaged smoke 正常
- [x] 窗口位置/大小记忆正常
- [x] 开机自启动可配置、后台拉取正常

---

## 第三阶段：体验打磨（第 5-6 周）

### 目标
达到可分发级别的完成度。

### 3.1 自动更新 (`electron/updater.ts`)
- [x] 集成 `electron-updater`，配置 GitHub Releases 作为更新源
- [x] 启动时检查更新、下载进度显示、安装确认

### 3.2 文件对话框集成
- [x] `web/src/components/accounts/ImportDialog.tsx` — 导入使用原生文件对话框，并保留 Web 文件选择 fallback
- [x] `web/src/pages/Accounts.tsx` — 账户导出使用原生保存对话框，并保留 Web 下载 fallback
- [x] `electron/preload.ts` 暴露 `desktopShell.openFileDialog()` / `desktopShell.saveFileDialog()`
- [x] `electron/file-dialog.ts` — 主进程处理原生打开/保存对话框并校验 IPC payload
- [x] `web/src/components/accounts/BackupRestore.tsx` — 数据库恢复使用原生 `.db` 文件选择；备份下载保留浏览器 Blob 路径避免大文件走 IPC

### 3.3 全局快捷键
- [x] `Ctrl+Shift+O` — 切换主窗口显示/隐藏
- [x] `Ctrl+Shift+M` — 手动拉取邮件

### 3.4 应用图标
- [x] 设计 `icon.ico`（16/32/48/256 尺寸）
- [x] 配置到 `electron-builder.yml`

### 3.5 安装包配置
- [x] NSIS 安装器（自定义安装路径）+ 便携版 zip 包
- [x] 文件关联 `.json` 账户导入文件
- [x] 支持拖拽 `.json` 文件到窗口直接导入

### 3.6 跨版本兼容性测试
- [x] Windows 10 测试不再执行（用户确认跳过）
- [x] Windows 11 测试通过（Windows 11 Pro 64 位 10.0.26200：packaged smoke + 回归测试通过）

### 第三阶段验证标准
- [ ] 自动更新、安装/卸载流程正常
  - [ ] 自动更新：发布前准备检查完成；GitHub Releases 为空且用户确认先不发布，真实端到端验证延后到发布时执行
  - [x] 安装/卸载：用户确认跳过，不执行本机安装/卸载验收
- [x] 全局快捷键、文件拖拽导入正常
  - [x] 全局快捷键：`Ctrl+Shift+O` / `Ctrl+Shift+M` 已通过测试、构建、打包和 packaged smoke
  - [x] 文件拖拽导入：`.json` 账户文件解析、导入对话框预填、Electron 测试、构建、打包和 packaged smoke 均已通过
- [x] Win10/Win11 兼容性范围已确认
  - [x] Win11：Windows 11 Pro 64 位 10.0.26200 已通过 packaged smoke 与回归测试
  - [x] Win10：用户确认不再检验

---

## 关键技术决策

| 决策点 | 方案 |
|--------|------|
| 前后端通信 | 保持 HTTP（前端 fetch → `localhost:<port>`） |
| OAuth2 回调 | 固定端口 `18000` + 启动时检测占用自动递增 |
| SQLite 路径 | `app.getPath('userData')` → `%APPDATA%/outlook-mail-manager/` |
| Token 安全 | `electron.safeStorage.encryptString()` 加密存储 |
| 构建工具 | Vite（前端）+ tsc（主进程/预加载） |

## 风险管理

| 风险 | 缓解措施 |
|------|----------|
| `better-sqlite3` 原生模块编译失败 | `electron-rebuild`；备选 `sql.js` |
| OAuth2 回调 URL 验证 | Azure AD 注册多个端口（18000-18005） |
| 杀毒软件误报 | 代码签名证书 + Windows Defender 白名单 |
| Web 版更新后桌面版需同步 | 共享代码库，仅 Electron 壳层独立 |

## 验证方式

1. **第一阶段**：开发模式启动 → 全功能手动回归测试 → 打包 exe 安装运行
2. **第二阶段**：逐个桌面功能验收（托盘、通知、窗口记忆、自启动）
3. **第三阶段**：安装/卸载流程测试 → Win10/Win11 兼容性测试 → 自动更新端到端测试
